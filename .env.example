# MCP Joke Server - Environment Configuration Example
# Copy this file to .env and update the values

# Required: Base URL for the jokes API
API_BASE_URL=https://official-joke-api.appspot.com

# Required: Authentication token for HTTP/SSE transports
# Generate a secure random token for production use
# Example: python -c "import secrets; print(secrets.token_urlsafe(32))"
LOCAL_TOKEN=your-secret-token-here

# Optional: MCP transport protocol
# Options: stdio (default), http, sse
MCP_PROTOCOL=stdio

# Optional: Server host for HTTP/SSE transports
MCP_SERVER_HOST=0.0.0.0

# Optional: Server port for HTTP/SSE transports
MCP_SERVER_PORT=8000

# Optional: Logging configuration
LOG_LEVEL=INFO
LOG_FILE=logs/mcp_server.log
LOG_ROTATION=10 MB
LOG_RETENTION=7 days

# Optional: Session configuration
SESSION_TIMEOUT=3600
SESSION_CLEANUP_INTERVAL=300

# Security Notes:
# - Never commit the actual .env file to version control
# - Use strong, randomly generated tokens in production
# - Rotate tokens periodically
# - Use HTTPS in production to protect tokens in transit
